package com.jk.catastrophic.data
/*


class CatResource<T>(var status: AuthStatus, var data: T?, var message: String?) {

    enum class AuthStatus {
        AUTHENTICATED, ERROR, LOADING*/
/*, NOT_AUTHENTICATED*//*

    }

    companion object {
        fun <T> authenticated(data: T?): CatResource<T> {
            return CatResource(AuthStatus.AUTHENTICATED, data, null)
        }

        fun <T> error(msg: String, data: T?): CatResource<T>? {
            return CatResource(AuthStatus.ERROR, data, msg)
        }

        fun <T> loading(data: T?): CatResource<T> {
            return CatResource(AuthStatus.LOADING, data, null)
        }

        */
/*  fun <T> logout(): CatResource<T?> {
              return CatResource(AuthStatus.NOT_AUTHENTICATED, null, null)
          }*//*

    }

}*/
